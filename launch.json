{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "启动后端服务器",
            "program": "${workspaceFolder}/server.js",
            "env": {
                "PORT": "3000",
                "MONGODB_URI": "mongodb://localhost:27017/english_learning"
            }
        }
    ]
}